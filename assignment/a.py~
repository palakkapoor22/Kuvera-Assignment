from DateTime import date 
file = open('new.txt')
fund_house=[]
for line in file:
	fields = line.strip().split(';')
	#print line
	fHouse=fields[1] 
	#nav=fields[4] 
	dt=date(fields[7])
	if fHouse not in fund_house:
		fund_house.append(fHouse)
print len(fund_house)
